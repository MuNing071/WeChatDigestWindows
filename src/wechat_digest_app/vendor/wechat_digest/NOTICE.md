This directory vendors selected source files from the upstream `cliffyan28/wechat-digest` project.

Included here:

- `crypto/`
- upstream license text in `LICENSE.upstream.txt`

The code is kept locally so this repository can function as a single clone without a nested Git dependency.
